---
layout: base
title: JavaScriptMN
changeFreq: daily
---

{# primary content #}
<div class="special-bg">
  <div class="container px-2 py-10 mx-auto mb-10">
    <div class="flex items-center justify-center">
      <div>
        <div
          class="flex flex-wrap justify-center px-4 text-center sm:flex-nowrap sm:px-0"
        >
          <div class="w-full sm:w-1/2">
            <div class="pl-8 sm:pl-16" data-animated-logo>
              {% include "_logo.svg" %}
            </div>
          </div>
          <div class="self-center w-full sm:w-1/2">
            <div class="text-4xl font-bold leading-normal"><h1>JavaScriptMN</h1></div>
            <p class="text-xl leading-normal">
              A monthly meetup for anyone interested in JavaScript and related
              technologies in the Twin Cities.
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

{# the next section #}

<div class="container px-2 mx-auto mb-10">
  <div class="flex items-center justify-center x-h-screen">
    <div class="w-full">
      {% include 'nextEvent.njk' %}
      <div>
        <div class="flex items-center justify-start mb-2 sm:justify-end">
          <h2
            class="inline-block px-4 text-2xl font-bold leading-normal text-white transform bg-black shadow -rotate-1 sm:rotate-1"
          >
            Links
          </h2>
        </div>
        <ul
          class="flex flex-wrap items-center justify-center space-y-2 sm:justify-end sm:space-y-0 sm:space-x-2 sm:flex-nowrap"
        >
          {% for link in actionLinks %}
          <li class="w-full sm:w-auto">
            <a
              class="block p-4 text-xl font-semibold transition-all duration-100 border-2 border-black hover:bg-jsmn-yellow"
              href="{{ link.href }}"
              >{{ link.text }}</a
            >
          </li>
          {% endfor %}
        </ul>
      </div>
    </div>
  </div>
</div>

{# location, speaking #}

<div class="container px-2 mx-auto mb-10">
  <div class="flex items-center justify-center x-h-screen">
    <div class="w-full">
      <section class="mb-8">
        <h2
          class="inline-block px-4 mb-2 text-2xl font-bold leading-normal text-white transform bg-black shadow -rotate-1"
        >
          Location
        </h2>
        <div
          class="flex flex-wrap items-center justify-between p-4 space-y-4 border-4 border-jsmn-yellow sm:flex-nowrap sm:space-y-0"
        >
          <div class="w-full">
            <p class="text-lg leading-7">
              We meet virtually on Zoom, RSVP to the event on Meetup in order to get a link!
            </p>
          </div>
        </div>
      </section>

      {# speaking #}
      <section class="mb-8">
        <div class="flex items-center justify-start mb-2">
          <h2
            class="inline-block px-4 text-2xl font-bold leading-normal text-white transform bg-black shadow -rotate-1"
          >
            Speak
          </h2>
        </div>
        <div
          class="flex flex-wrap items-center justify-between p-4 space-y-4 border-4 border-jsmn-yellow sm:flex-nowrap sm:space-y-0"
        >
          <div>
            <h3 class="text-2xl font-bold">Interested in speaking?</h3>
            <p class="text-lg">
              We are always open to new speakers, please see our questionnaire
              for more details.
            </p>
          </div>
          <div class="w-full sm:w-auto">
            <a
              class="flex items-center p-4 text-xl font-semibold text-center transition-all duration-100 border-2 border-black lg:text-left hover:bg-jsmn-yellow justify-evenly"
              href="{{ '/speak/' | url }}"
            >
              Speak at JSMN!
            </a>
          </div>
        </div>
      </section>

      {# promoted sponsors #}
      <section class="mb-8">
        <div class="flex items-center justify-between mb-2">
          <h2
            class="inline-block px-4 text-2xl font-bold leading-normal text-white transform bg-black shadow -rotate-1"
          >
            Sponsors
          </h2>
          <p>
            <a class="hover:underline" href="{{ '/sponsors/' | url }}">
              More Sponsors →
            </a>
          </p>
        </div>
        <div class="p-4 border-4 border-jsmn-yellow">
          {% for section in sponsors %} {% if section.showOnHome === true %}
          <div class="w-2/3 mx-auto mb-4">
            <h3 class="text-2xl font-bold">{{ section.section | safe }}</h3>
            <div
              class="grid grid-cols-none space-y-4 md:grid-cols-2 lg:grid-cols-4 sm:gap-2 sm:space-y-0"
            >
              {% for sponsor in section.sponsors %}
              <div class="flex items-center justify-center">
                <a
                  href="{{ sponsor.href }}"
                  target="_blank"
                  rel="noopener noreferrer"
                  class="flex items-center justify-center w-full p-4 transition-all border border-gray-200 shadow hover:border-gray-600"
                >
                  <img
                    src="{{ sponsor.imgSrc | url }}"
                    alt="{{ sponsor.title }}"
                    title="{{ sponsor.title }}"
                    class="h-32"
                  />
                </a>
              </div>
              {% endfor %}
            </div>
          </div>
          {% endif %} {% endfor %}
        </div>
      </section>
    </div>
  </div>
</div>
