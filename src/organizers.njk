---
layout: base
title: Organizers
---

<div class="container max-w-5xl p-2 mx-auto my-8">
  <div class="flex items-center justify-center x-h-screen">
    <div class="w-full">
      <div class="mb-8">
        <div class="flex flex-col items-center justify-center mb-2 space-y-2">
          <h1
            class="inline-block px-4 text-3xl font-bold text-white transform bg-black shadow -rotate-1"
          >
            Our Organizers
          </h1>
          {# <p>add blurb here if needed</p> #}
        </div>
        <div class="p-4 border-4 border-jsmn-yellow">
          <div class="grid content-center justify-center grid-cols-none gap-4 sm:grid-cols-2 md:auto-cols-auto justify-items-center alternate-tilt">
            {# add more organizers in src/globals/organizers.js #}
            {% for organizer in organizers %}
              <div class="flex flex-col items-center justify-center space-y-2">
                <img alt="{{ organizer.name }}" src="{{ organizer.avatar | url }}" class="block object-cover w-64 rounded-full aspect-square" />
                <p class="inline-block shadow px-4 mb-2 text-xl font-bold text-white transform bg-black {% if loop.index % 2 == 0 %} rotate-1 {% else %} -rotate-1 {% endif %}">{{ organizer.name }}</p>
              </div>
            {% endfor %}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
